=form_for current_user.relationships.build(:followed_id => @company.id), 
  :remote => true do |f|
  %div= f.hidden_field :followed_id
  .actions.follow
    =f.submit
    %span.liloader=image_tag('loader.gif')
